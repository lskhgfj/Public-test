name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-lates
    steps: 
      - run: echo "Hello World! The job was automatically triggered by a ${{github.event.name }}"
